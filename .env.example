# Database Configuration
DB_USER=claim_user
DB_PASSWORD=claim_password
DB_NAME=claim_db
DB_PORT=5432

# API Configuration
API_PORT=8000
CORS_ORIGINS=*

# Deployment
BRANCH=main
DEPLOY_CHECK_INTERVAL=60
COMPOSE_FILE=docker-compose.prod.yml

# Optional: Reverse Proxy
DOMAIN=claim.example.com
LETSENCRYPT_EMAIL=admin@example.com

# Optional: SSL/TLS (if using Traefik or similar)
SSL_ENABLED=false
